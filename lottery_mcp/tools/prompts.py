"""
MCP Server Prompts - 分析提示模板

Prompts 是预定义的模板，引导 LLM 正确使用工具。
本模块提供彩票分析相关的提示模板。

提示模板 URI 格式:
- lottery://prompts/analyze_match - 单场比赛分析流程
- lottery://prompts/analyze_parlay - 串关投注分析流程
- lottery://prompts/mixed_parlay_guide - 混合过关指南
- lottery://prompts/value_betting - 价值投注分析流程
"""

import logging
from typing import Any, Dict, List, Optional

logger = logging.getLogger("lottery_mcp")


def register_prompts(mcp):
    """注册所有提示词
    
    Args:
        mcp: FastMCP 实例
    """
    
    # ============================================================
    # 单场比赛分析提示
    # ============================================================
    @mcp.prompt(
        name="analyze_match",
        description="单场比赛分析流程提示模板，引导LLM正确调用分析工具",
    )
    def analyze_match_prompt(
        match_id: str,
        home_team: str = "主队",
        away_team: str = "客队",
        lottery_type: str = "竞彩足球",
    ) -> str:
        return f"""# 单场比赛分析流程

请按以下步骤分析比赛 {match_id}（{home_team} vs {away_team}）：

## 第一步：获取比赛数据
使用 `lottery_fetch_today_matches` 获取比赛列表，确认比赛信息。

## 第二步：获取赔率数据
使用 `lottery_get_market_odds` 获取该场比赛的赔率信息。

## 第三步：进行统计分析
使用 `lottery_analyze_match` 进行深度分析，推荐参数：
- match_id: "{match_id}"
- lottery_type: "{lottery_type}"
- analysis_depth: "deep" （深度分析）
- include_market_odds: true

## 第四步：分析五大玩法
使用 `lottery_analyze_match_plays` 获取五大玩法的分析结果：
- SPF（胜平负）
- RQSPF（让球胜平负）
- BF（比分）
- ZJQ（总进球）
- BQC（半全场）

## 第五步：综合评估
根据分析结果，给出：
1. 最推荐的玩法和选项
2. 价值投注机会（期望值 > 1.0）
3. 风险提示

## 注意事项
- 竞彩足球返还率为 70%，期望值计算不需要乘返还率
- 比分玩法最多 4 关，半全场最多 4 关
- 让球胜平负的让球数为整数（1, 2, 3...）
"""

    # ============================================================
    # 串关投注分析提示
    # ============================================================
    @mcp.prompt(
        name="analyze_parlay",
        description="串关投注分析流程提示模板，包含验证和奖金计算",
    )
    def analyze_parlay_prompt(
        match_count: int = 3,
        lottery_type: str = "竞彩足球",
    ) -> str:
        return f"""# 串关投注分析流程

请按以下步骤分析 {match_count} 场比赛的串关投注：

## 第一步：验证串关合法性
使用 `lottery_validate_parlay` 验证串关组合是否合规：
- 检查场次数量（1-8场，胜平负/让球胜平负支持单关）
- 检查串关类型是否支持
- 检查总投注金额（竞彩≤6000元，北单≤20000元）
- 检查倍数范围（竞彩2-50倍，北单2-99倍）

## 第二步：检查混合过关规则
如果是混合过关（多种玩法组合），`lottery_validate_parlay` 会自动检测并验证：
- 检查同一场次是否选择了多个玩法（禁止）
- 检查木桶原则（关数受最低玩法限制）

## 第三步：计算预期奖金
使用 `lottery_calculate_bonus` 计算预期奖金：
- 竞彩过关：2元 × 各场固定奖金连乘 × 倍数
- 北单：2元 × SP值连乘 × 65% × 倍数

## 第四步：查询规则详情
使用 `lottery_query_rules` 查询相关规则：
- rule_type: "limits" - 限额规则
- rule_type: "parlay" - 串关规则
- rule_type: "bonus" - 奖金规则

## M串N组合参考
竞彩官方组合（{lottery_type}）：
- M串1: 2x1, 3x1, 4x1, 5x1, 6x1, 7x1, 8x1
- M串N容错: 3x3, 3x4, 4x4, 4x5, 4x6, 4x11, 5x5, 5x6, 5x10, 5x16, 5x20, 5x26...

## 注意事项
- 竞彩单票限额 6000 元
- 倍数范围 2-50 倍（竞彩）/ 2-99 倍（北单）
- 单注奖金 ≥ 1万元扣 20% 税
"""

    # ============================================================
    # 混合过关指南
    # ============================================================
    @mcp.prompt(
        name="mixed_parlay_guide",
        description="混合过关专项指南，包含木桶原则和限制条件",
    )
    def mixed_parlay_guide_prompt() -> str:
        return """# 混合过关投注指南

## 什么是混合过关？
混合过关允许在同一次串关投注中选择不同玩法：
- 胜平负（SPF）
- 让球胜平负（RQSPF）
- 比分（BF）
- 总进球（ZJQ）
- 半全场（BQC）

## 三大核心规则

### 规则一：同一场次不可多玩法
❌ 错误示例：A场比赛同时选择"胜平负"和"比分"
✅ 正确示例：A场选择"胜平负"，B场选择"比分"

### 规则二：不同运动项目不能混合
❌ 错误示例：足球胜平负 + 北单胜负
✅ 正确示例：全部为足球比赛

### 规则三：木桶原则
最大串关数 = 所选玩法中限制最低者

| 玩法 | 最大关数 |
|------|----------|
| 胜平负 | 8关 |
| 让球胜平负 | 8关 |
| 总进球 | 6关 |
| 比分 | 4关 |
| 半全场 | 4关 |

示例：选择胜平负(8关) + 比分(4关) → 最多只能串4关

## 使用工具验证
1. 使用 `lottery_validate_parlay` 验证串关（自动检测混合过关）
2. 使用 `lottery_query_rules` 查询玩法组合限制（rule_type: "parlay"）

## 策略建议
- 利用混合过关优势：高概率玩法（胜平负）+ 高赔率玩法（比分）
- 注意木桶原则：避免被低关数玩法限制
- 合理分配：不要为了混合而混合
"""

    # ============================================================
    # 价值投注分析提示
    # ============================================================
    @mcp.prompt(
        name="value_betting",
        description="价值投注分析流程，识别期望值>1的投注机会",
    )
    def value_betting_prompt() -> str:
        return """# 价值投注分析流程

## 什么是价值投注？
价值投注是指期望值（Expected Value, EV）大于 1 的投注机会。

计算公式：
- EV = 概率 × 赔率
- 当 EV > 1 时，长期来看有利可图

## 分析步骤

### 第一步：获取比赛分析
使用 `lottery_analyze_match` 获取各选项的概率估计

### 第二步：获取赔率
使用 `lottery_get_market_odds` 获取各选项的赔率

### 第三步：计算期望值
对每个选项计算 EV = 概率 × 赔率

### 第四步：筛选价值投注
- EV > 1.15：高价值
- EV > 1.0：有价值
- EV < 1.0：无价值

## 使用工具
使用 `lottery_find_value_bets` 可直接获取价值投注推荐
使用 `lottery_analyze_match_plays` 获取五大玩法的价值投注分析

## 注意事项
- 竞彩返还率 70%，意味着平均期望值 < 1
- 价值投注需要准确的概率估计
- 单次投注结果具有随机性，长期才能体现价值
- 理性购彩，控制风险
"""

    # ============================================================
    # 规则查询提示
    # ============================================================
    @mcp.prompt(
        name="query_rules",
        description="规则查询流程提示，引导用户正确查询规则",
    )
    def query_rules_prompt() -> str:
        return """# 彩票规则查询指南

## 可查询的规则类型

使用 `lottery_query_rules` 工具，支持以下 rule_type：

| rule_type | 说明 |
|-----------|------|
| limits | 投注限额规则（单注最低/最高） |
| parlay | 串关规则（场次限制、类型支持、混合过关规则） |
| play | 玩法规则（支持的玩法列表） |
| bonus | 奖金规则（返还率、税率、封顶） |
| mixed_parlay | 混合过关专项规则 |

## 直接读取规则资源

使用 Resources 直接读取完整规则：
- `lottery://rules/jingcai` - 竞彩足球完整规则
- `lottery://rules/beidan` - 北京单场完整规则
- `lottery://rules/ctzc` - 传统足彩完整规则
- `lottery://rules/mixed_parlay` - 混合过关专项规则
- `lottery://rules/errors` - 错误码映射表

## 常见问题快速解答

### 竞彩足球
- 返还率：70%
- 最大串关：8场
- 单票限额：6000元
- 倍数范围：2-50倍
- 让球类型：整数让球

### 北京单场
- 返还率：65%
- 最大串关：15场
- 单票限额：20000元
- 倍数范围：2-99倍
- 独有玩法：上下单双、胜负过关

### 税金规则
- 单注奖金 ≥ 1万元扣 20% 税
- 封顶：单关10万, 2-3关20万, 4-5关50万, 6+关100万
"""

    # ============================================================
    # 投注单生成提示
    # ============================================================
    @mcp.prompt(
        name="generate_betting_slips",
        description="投注单生成流程提示，包含策略选择和金额控制",
    )
    def generate_betting_slips_prompt() -> str:
        return """# 投注单生成流程

## 使用工具
使用 `lottery_generate_betting_slips` 生成投注单

## 策略选择

| strategy | 说明 | 适用场景 |
|----------|------|----------|
| single | 单关投注 | 高信心单场 |
| parlay | 串关投注 | 多场组合 |
| mixed | 混合过关 | 多玩法组合 |
| auto_parlay | 自动串关推荐 | 系统智能推荐 |

## 参数说明
- match_ids: 比赛ID列表（1-8场）
- bankroll: 总投注预算
- lottery_type: 彩种类型
- parlay_type: 串关类型（2x1, 3x1, 4x11 等）
- max_matches: 最大串关场次数
- min_confidence: 最低置信度阈值

## 金额控制
- 竞彩单票限额：6000元
- 北单单票限额：20000元
- 倍数范围：竞彩2-50倍，北单2-99倍
- 单注金额：2元

## 使用凯利公式
使用 `lottery_generate_kelly_slips` 基于凯利公式计算最优投注比例

## 注意事项
1. 生成投注单后，使用 `lottery_validate_parlay` 验证合法性
2. 使用 `lottery_calculate_bonus` 计算预期收益
3. 理性购彩，量力而行
"""

    # ============================================================
    # AI推理框架：战略分析
    # ============================================================
    @mcp.prompt(
        name="strategic_analysis",
        description="战略分析框架：引导AI进行系统性比赛分析",
    )
    def strategic_analysis_prompt() -> str:
        return """# 战略分析框架

作为专业彩票分析师，请基于以下框架进行系统性分析：

## 信息收集阶段
1. 使用 `lottery_get_match_context` 获取比赛全维度上下文
2. 使用 `lottery_get_live_scores` 获取实时比赛数据（如比赛进行中）
3. 使用 `lottery_get_market_sentiment` 了解市场情绪

## 推理分析阶段
4. 使用 `lottery_predict_with_model` 获取模型预测概率
5. 使用 `lottery_find_value_bets` 识别价值投注机会
6. 使用 `lottery_track_odds_changes` 追踪赔率变化，检测异常信号
7. 使用 `lottery_get_injury_suspension` 评估伤停影响

## 风险评估阶段
8. 使用 `lottery_assess_risk` 进行多维度风险评估
9. 使用 `lottery_analyze_match` 模拟关键情景

## 决策形成阶段
10. 使用 `lottery_get_daily_recommendations` 生成综合建议
11. 【强制】使用 `lottery_validate_scenario` 验证建议是否符合规则

## 输出要求
- 每个结论必须有数据支撑
- 明确标注置信度（高/中/低）
- 列出关键假设和不确定性
- 提供备选方案

【规则底线】任何建议必须通过规则验证，不可违反硬限制（限额、串关规则等）。
"""

    # ============================================================
    # AI推理框架：逆向思维
    # ============================================================
    @mcp.prompt(
        name="contrarian_thinking",
        description="逆向思维框架：挑战主流观点，发现被低估的机会",
    )
    def contrarian_thinking_prompt() -> str:
        return """# 逆向思维分析框架

请用逆向思维分析这场比赛，挑战主流观点：

## 第一步：识别主流观点
1. 使用 `lottery_get_market_sentiment` 了解市场热门选项
2. 使用 `lottery_track_odds_changes` 分析赔率变化方向（大众资金流向）
3. 查看媒体/专家预测倾向

## 第二步：寻找反方证据
4. 使用 `lottery_get_match_context` 获取完整数据
5. 重点关注与主流观点矛盾的数据：
   - 历史交锋中弱势方的优势
   - 近期状态被忽视的细节
   - 伤停对强队的隐性影响
6. 使用 `lottery_track_odds_changes` 发现异常赔率变动信号

## 第三步：独立思考
7. 假设主流观点是错的，什么情况下会成立？
8. 使用 `lottery_analyze_match` 测试反方情景
9. 使用 `lottery_find_value_bets` 计算逆向选择的期望值

## 第四步：风险评估
10. 使用 `lottery_assess_risk` 评估逆向选择的风险
11. 如果逆向选择错了，损失有多大？

## 输出要求
- 明确列出主流观点和逆向观点
- 为每个观点提供证据支撑
- 给出逆向选择的合理性和风险
- 最终决策必须基于证据，而非情绪

【规则底线】逆向分析不能违反硬限制（限额、串关规则等）。
"""

    # ============================================================
    # AI推理框架：概率校准
    # ============================================================
    @mcp.prompt(
        name="probability_calibration",
        description="概率校准框架：避免过度自信或过度保守",
    )
    def probability_calibration_prompt() -> str:
        return """# 概率校准框架

请校准你的概率判断，避免认知偏差：

## 常见认知偏差检查

### 1. 过度自信偏差
- 你是否过于相信自己的判断？
- 使用 `lottery_predict_with_model` 对比模型预测和你的直觉
- 如果差异>15%，重新审视你的假设

### 2. 锚定效应
- 你是否被初始赔率过度影响？
- 使用 `lottery_find_value_bets` 识别价值投注机会
- 对比市场赔率，识别锚定偏差

### 3. 可得性偏差
- 你是否过度依赖最近的比赛结果？
- 使用 `lottery_get_match_context` 查看完整历史交锋
- 使用Dixon-Coles时间衰减模型（模型已内置）

### 4. 确认偏误
- 你是否只寻找支持自己观点的证据？
- 强制列出3个反方证据
- 使用 `lottery_track_odds_changes` 发现被忽视的信号

## 校准步骤

1. **初始估计**：给出你的直觉概率
2. **模型对比**：使用 `lottery_predict_with_model` 获取模型概率
3. **差异分析**：解释差异原因（信息优势 vs 认知偏差）
4. **区间估计**：给出概率区间（如40%-50%），而非点估计
5. **置信度评估**：使用 `lottery_assess_risk` 评估整体置信度

## 输出要求
- 明确标注初始估计和校准后估计
- 列出识别到的认知偏差
- 给出概率区间而非单一数值
- 说明关键不确定性

【规则底线】概率校准不能改变硬限制（限额、串关规则等）。
"""

    # ============================================================
    # AI推理框架：基于模型的预测
    # ============================================================
    @mcp.prompt(
        name="model_based_prediction",
        description="基于模型的预测框架：结合多个模型的预测结果",
    )
    def model_based_prediction_prompt() -> str:
        return """# 基于模型的预测框架

请使用多个模型进行预测，并综合各模型的结果：

## 第一步：获取模型预测
1. 使用 `lottery_predict_with_model` 获取集成模型预测
2. 分别获取泊松模型、Elo模型、xG模型的独立预测
3. 记录各模型的置信度和关键特征

## 第二步：模型对比分析
- 各模型预测是否一致？
- 如果存在分歧，可能的原因是什么？
- 哪个模型对该类型比赛更准确？

## 第三步：综合判断
- 基于多模型结果形成综合预测
- 给出概率区间而非点估计
- 明确标注不确定性来源

## 第四步：验证假设
- 检查模型假设是否成立
- 考虑模型未覆盖的因素

【重要提醒】模型输出是概率，不是保证。请始终结合其他分析工具。
"""

    # ============================================================
    # AI推理框架：贝叶斯更新
    # ============================================================
    @mcp.prompt(
        name="bayesian_update",
        description="贝叶斯更新框架：根据新证据修正先验概率",
    )
    def bayesian_update_prompt() -> str:
        return """# 贝叶斯更新框架

请使用贝叶斯思维，根据新证据不断修正预测：

## 初始先验（赛前）
1. 使用 `lottery_predict_with_model` 获取初始概率
2. 这是基于历史数据的先验概率

## 新证据收集
比赛前/比赛中收集新证据：
- `lottery_get_live_scores` - 实时比赛数据
- `lottery_get_market_sentiment` - 市场情绪变化
- `lottery_track_odds_changes` - 赔率异常信号

## 贝叶斯更新
对每个新证据：
- 评估该证据的可靠性
- 计算似然度 P(证据|假设)
- 更新后验概率 P(假设|证据)

## 更新公式
后验概率 ∝ 先验概率 × 似然度

## 输出要求
- 展示更新前后的概率变化
- 说明哪些证据对概率影响最大
- 给出最终修正后的预测

【示例】
初始：主队获胜概率 60%
新证据：主队核心球员伤愈首发
更新后：主队获胜概率 65%（+5%）
"""

    # ============================================================
    # 场景Prompt：开奖结果查询
    # ============================================================
    @mcp.prompt(
        name="query_results",
        description="开奖结果查询场景：引导查询历史开奖数据并分析结果",
    )
    def query_results_prompt() -> str:
        return """# 开奖结果查询

## 使用场景
需要查询历史开奖结果、中奖信息或开奖统计数据时使用。

## 查询步骤

### 第一步：查询历史开奖记录
使用 `lottery_query_history` 获取历史开奖数据：
- 指定日期范围（start_date, end_date）
- 指定彩种类型（lottery_type）
- 支持分页查询（page, page_size 参数）
  - 默认每页 20 条记录
  - 最大每页 100 条记录
  - 使用 page 参数翻页（从第1页开始）

### 第二步：分析开奖结果
使用 `lottery_analyze_results` 对开奖数据进行统计分析：
- 中奖概率分布
- 冷热号统计
- 奖金走势分析

## 分页使用说明
当查询结果较多时，请按以下方式分页获取：
1. 首次查询使用 page=1, page_size=20
2. 根据返回的 total_count 判断总页数
3. 逐页获取剩余数据（page=2, page=3...）
4. 避免一次性请求过多数据

## 注意事项
- 开奖数据通常在比赛结束后 30 分钟内更新
- 历史数据查询建议限制日期范围以提高查询速度
- 中奖金额以官方公布为准
"""

    # ============================================================
    # 场景Prompt：实时比分追踪
    # ============================================================
    @mcp.prompt(
        name="live_tracking",
        description="实时比分追踪场景：引导获取实时比赛数据和赔率变化",
    )
    def live_tracking_prompt() -> str:
        return """# 实时比分追踪

## 使用场景
比赛进行中，需要实时跟踪比分变化、赔率波动和风险评估时使用。

## 追踪步骤

### 第一步：获取实时比分
使用 `lottery_get_live_scores` 获取当前正在进行的比赛实时数据：
- 比分、比赛时间、红黄牌
- 进球事件、换人信息

### 第二步：追踪赔率变化
使用 `lottery_track_odds_changes` 监控赔率实时变动：
- 主胜/平/客胜赔率走势
- 异常赔率变动检测
- 市场资金流向分析

### 第三步：实时风险评估
使用 `lottery_assess_risk` 根据实时数据更新风险评估：
- 比分变化对投注的影响
- 赔率变动带来的风险提示
- 是否需要调整投注策略

## 实时数据更新频率说明
- 比分数据：比赛进行中每 30 秒更新一次
- 赔率数据：随市场变动实时更新，通常每 1-2 分钟有变化
- 风险评估：基于最新数据实时计算
- 建议在关键时刻（进球、红牌等）后重新获取数据

## 注意事项
- 实时数据可能存在短暂延迟，以官方数据为准
- 比赛中赔率波动较大，注意控制风险
- 已出票的投注无法更改，请理性对待比赛中的变化
"""

    # ============================================================
    # 场景Prompt：投注记录与复盘
    # ============================================================
    @mcp.prompt(
        name="betting_review",
        description="投注记录与复盘场景：引导记录投注并进行系统复盘",
    )
    def betting_review_prompt() -> str:
        return """# 投注记录与复盘

## 使用场景
需要记录投注历史、统计投注数据、进行投注复盘和策略优化时使用。

## 操作步骤

### 第一步：记录投注
使用 `lottery_track_bet` 记录每一笔投注：
- 比赛信息（match_id, home_team, away_team）
- 投注内容（玩法、选项、金额）
- 投注结果（won/lost/pending）
- 赔率和奖金

### 第二步：获取投注统计
使用 `lottery_get_betting_stats` 获取投注统计数据：
- 总投注金额、总中奖金额
- 胜率、盈亏比
- 各玩法命中率
- 时间维度趋势（日/周/月）

## 投注纪律要点
1. **设定预算上限**：每日投注不超过预算的 10%
2. **严格执行止损**：当日亏损达到止损线立即停止
3. **避免情绪化投注**：连续亏损后冷静休息，不追号回本
4. **记录每笔投注**：养成记录习惯，便于复盘分析
5. **定期复盘**：每周/每月回顾投注记录，总结经验教训

## 复盘要点
- 哪些投注决策是正确的？为什么？
- 哪些投注存在偏差？如何改进？
- 是否遵守了投注纪律？
- 期望值（EV）计算是否准确？
- 下阶段需要调整什么策略？

## 注意事项
- 投注记录是复盘的基础，务必准确完整
- 复盘应客观冷静，不要只看盈利忽略亏损
- 长期盈利的关键在于纪律和概率优势
"""

    # ============================================================
    # 场景Prompt：风控与合规
    # ============================================================
    @mcp.prompt(
        name="risk_compliance",
        description="风控与合规场景：引导执行规则验证和风控检查",
    )
    def risk_compliance_prompt() -> str:
        return """# 风控与合规

## 使用场景
需要验证投注方案是否合规、执行风控检查、拦截违规操作时使用。

## 风控步骤

### 第一步：场景验证
使用 `lottery_validate_scenario` 验证投注场景的合规性：
- 单日/单注限额检查
- 串关场次限制检查
- 年龄限制检查
- 场景特定规则检查

### 第二步：规则守卫检查
使用 `lottery_rule_guard` 执行多阶段规则检查：
- pre_bet（投注前）：检查限额、串关场次、赔率范围
- post_bet（投注后）：验证数据有效性、记录投注历史
- daily（日检查）：单日累计限额、投注次数、亏损止损
- emergency（紧急）：异常行为检测

### 第三步：违规拦截
使用 `lottery_enforce_constraints` 对严重违规操作执行强制拒绝：
- 明确记录拒绝原因
- 标注违规场景类型
- 用于后续审计追踪

## 理性购彩提醒
- 彩票是娱乐方式，不是投资手段
- 请设定合理的购彩预算，量力而行
- 未满 18 周岁不得购彩
- 不要借贷购彩，不要影响正常生活
- 如有购彩困扰，请寻求专业帮助

## 注意事项
- 风控规则为硬性限制，不可绕过
- 所有投注方案必须通过场景验证
- 系统会自动记录违规行为用于审计
- 风控参数可通过系统配置调整
"""

    logger.info("已注册 %d 个 MCP Prompts", 15)
